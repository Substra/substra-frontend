apiVersion: v1
kind: Service
metadata:
  name: {{ include "connect-frontend.fullname" . }}
  labels:
    {{- include "connect-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "connect-frontend.selectorLabels" . | nindent 4 }}
