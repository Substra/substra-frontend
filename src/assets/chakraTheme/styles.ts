import { Colors, Fonts, Spaces, zIndexes } from '../theme';

const styles = {
    colors: Colors,
    space: Spaces,
    fontSizes: Fonts.sizes,
    fontWeights: Fonts.weights,
    zIndices: zIndexes,
};

export default styles;
