import React from 'react';
import List from '../../../../common/components/list/redux';
import Metadata from './components/metadata';

export default props => <List Metadata={Metadata} {...props} />;
